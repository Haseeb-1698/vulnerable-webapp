apiVersion: v1
kind: ConfigMap
metadata:
  name: vulnerable-webapp-config
  namespace: vulnerable-webapp
data:
  NODE_ENV: "production"
  PORT: "3001"
  UPLOAD_MAX_SIZE: "10485760"
  RATE_LIMIT_WINDOW: "900000"
  RATE_LIMIT_MAX: "100"
  VITE_APP_NAME: "Vulnerable Task Manager"
  VITE_APP_VERSION: "1.0.0"
